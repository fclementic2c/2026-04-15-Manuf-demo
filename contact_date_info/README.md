# Contact Date Extension

This module extends the Odoo Contact (res.partner) model to include a "Contact Date" field.

## Features
- Adds `contact_date` field to `res.partner`.
- Displays the field in the Contact form view below the Name.

## Technical Information
- Targeting Odoo 19.0.
- Depends on `base` and `contacts`.
